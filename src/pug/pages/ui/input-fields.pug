extends ../../layouts/ui-layout/ui-layout.pug

block variables
	- var title = 'Поля ввода - UI-kit и HTML фреймворк - Документация';
	- var pageClass = 'body--dashboard';

block content
	.main-wrapper
		.container.container--left.user-content.pt-35
			h1 Поля ввода
			.section-ui
				+input('name','text','Введите имя','Поле для ввода')
			pre.
				+input('name','text','Введите имя','Поле для ввода')
			
			.title mixins/_input.pug
			pre.
				mixin input(name,type,placeholder,labelText)
					label.label-class #{'#{labelText}'}
						if placeholder
							input.input.placeholder(type=type name=name placeholder=placeholder)
						else
							input.input(type="text" name=name)
			.section-ui
				+textarea('name','','Парк находится в горах Сьерра-Невада на востоке штата Калифорния и занимает огромную даже для Америки площадь, около 3000 кв. км.','Сообщение - Textarea')
					
			pre.
				+textarea('name','','Парк находится в горах Сьерра-Невада на востоке штата Калифорния и занимает огромную даже для Америки площадь, около 3000 кв. км.','Сообщение - Textarea')
			
			.title mixins/_textarea.pug
			pre.
				mixin textarea(name,placeholder,areaText,labelText)
					label.label-class #{'#{labelText}'}
						if placeholder
							textarea.textarea.placeholder(name=name placeholder=placeholder) #{'#{areaText}'}
						else
							textarea.textarea(name=name) #{'#{areaText}'}

			p Параметры: name - содержание атрибута name, type - содержание атрибута type, placeholder - содержание атрибута placeholder, labelText - текст в теге label, areaText - текст в теге textarea.
